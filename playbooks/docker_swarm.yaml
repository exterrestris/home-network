---
###
# provision-02-docker-swarm playbook
##
# Set up the Swarm cluster and put it into the desired state
#
# Playbook inspired by https://github.com/nextrevision/ansible-swarm-playbook
# Big thanks to John Patterson for his work!
##

# use docker_swarm_advertise_addr instead of iface when ethX has multiple IP addresses
- hosts: docker_swarm
  tasks:
    - name: define docker_swarm_advertise_addr_string (defined)
      set_fact:
        docker_swarm_advertise_addr_string: "{{ docker_swarm_advertise_addr }}"
      when: docker_swarm_advertise_addr is defined
    - name: define docker_swarm_advertise_addr_string (defined)
      set_fact:
        docker_swarm_advertise_addr_string: "{{ docker_swarm_iface | default('eth0') }}"
      when: docker_swarm_advertise_addr is not defined

# determine the status of each manager node and break them
# into two groups:
#   - swarm_manager_operational (swarm is running and active)
#   - swarm_manager_bootstrap (host needs to be joined to the cluster)
- hosts: swarm_manager
  tasks:
    - name: load docker info as facts
      docker_info_facts:
      changed_when: False

    - name: create swarm_manager_operational group
      add_host:
        hostname: "{{ item }}"
        groups: swarm_manager_operational
      with_items: "{{ ansible_play_hosts | default(play_hosts) }}"
      when: hostvars[item]['docker_info']['Swarm']['LocalNodeState'] == 'active'
      run_once: true
      changed_when: False

    - name: create swarm_manager_bootstrap group
      add_host:
        hostname: "{{ item }}"
        groups: swarm_manager_bootstrap
      with_items: "{{ ansible_play_hosts | default(play_hosts) }}"
      when: hostvars[item]['docker_info']['Swarm']['LocalNodeState'] != 'active'
      run_once: true
      changed_when: False

# determine the status of each worker node and break them
# into two groups:
#   - swarm_worker_operational (host is joined to the swarm cluster)
#   - swarm_worker_bootstrap (host needs to be joined to the cluster)
- hosts: swarm_worker
  tasks:
    - name: load docker info as facts
      docker_info_facts:
      changed_when: False

    - name: create swarm_worker_operational group
      add_host:
        hostname: "{{ item }}"
        groups: swarm_worker_operational
      with_items: "{{ ansible_play_hosts | default(play_hosts) }}"
      when: hostvars[item]['docker_info']['Swarm']['LocalNodeState'] == 'active'
      run_once: true
      changed_when: False

    - name: create swarm_worker_bootstrap group
      add_host:
        hostname: "{{ item }}"
        groups: swarm_worker_bootstrap
      with_items: "{{ ansible_play_hosts | default(play_hosts) }}"
      when: hostvars[item]['docker_info']['Swarm']['LocalNodeState'] != 'active'
      run_once: true
      changed_when: False

# when the swarm_manager_operational group is empty, meaning there
# are no hosts running swarm, we need to initialize one of the hosts
# then add it to the swarm_manager_operational group
- hosts: swarm_manager_bootstrap[0]
  tasks:
    - name: initialize swarm cluster
      command: >
        docker swarm init
        --advertise-addr={{ docker_swarm_advertise_addr_string | default('eth0') }}:2377
      when: "'swarm_manager_operational' not in groups"
      register: bootstrap_first_node

    - name: add initialized host to swarm_manager_operational group  # noqa 503
      add_host:
        hostname: "{{ item }}"
        groups: swarm_manager_operational
      with_items: "{{ ansible_play_hosts | default(play_hosts) }}"
      when: bootstrap_first_node.changed

# retrieve the swarm tokens and populate a list of ips listening on
# the swarm port 2377
- hosts: swarm_manager_operational[0]
  vars:
    iface: "{{ docker_swarm_iface | default('eth0') }}"
  tasks:
    - name: retrieve swarm manager token
      command: docker swarm join-token -q manager
      register: docker_swarm_manager_token
      changed_when: False

    - name: retrieve swarm worker token
      command: docker swarm join-token -q worker
      register: docker_swarm_worker_token
      changed_when: False

    - name: populate list of manager ips from docker_swarm_advertise_addr
      add_host:
        hostname: "{{ docker_swarm_advertise_addr }}"
        groups: docker_swarm_manager_ips
      when: docker_swarm_advertise_addr is defined
      changed_when: False

    - name: populate list of manager ips from iface
      add_host:
        hostname: "{{ hostvars[item]['ansible_' + iface]['ipv4']['address'] }}"
        groups: docker_swarm_manager_ips
      when: docker_swarm_advertise_addr is not defined
      with_items: "{{ ansible_play_hosts | default(play_hosts) }}"
      changed_when: False

# join the hosts not yet initialized to the swarm cluster
- hosts: swarm_manager_bootstrap:!swarm_manager_operational
  vars:
    token: "{{ hostvars[groups['swarm_manager_operational'][0]]['docker_swarm_manager_token']['stdout'] }}"
  tasks:
    - name: join manager nodes to cluster # noqa 301
      command: >
        docker swarm join
        --advertise-addr={{ docker_swarm_advertise_addr_string | default('eth0') }}:2377
        --token={{ token }}
        {{ groups['docker_swarm_manager_ips'][0] }}:2377

# join the remaining workers to the swarm cluster
- hosts: swarm_worker_bootstrap
  vars:
    token: "{{ hostvars[groups['swarm_manager_operational'][0]]['docker_swarm_worker_token']['stdout'] }}"
  tasks:
    - name: join worker nodes to cluster # noqa 301
      command: >
        docker swarm join
        --advertise-addr={{ docker_swarm_advertise_addr_string | default('eth0') }}
        --token={{ token }}
        {{ groups['docker_swarm_manager_ips'][0] }}:2377
