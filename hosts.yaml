all:
  hosts:
    spacedock:
  children:
    nas:
      hosts:
        memory_alpha:
        memory_beta:
    vm:
      hosts:
        memory_alpha:
    raspberry_pi:
      hosts:
        memory_beta: